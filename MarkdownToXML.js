var index = 0;
var word = [];
var Lexer = function(wordLine) {
	var parts;	

	parts = wordLine.charAt(i);
	while(parts != null) {
		match(parts, wordLine, index);
		index++;
	}

}

var match = function(parts, wordLine, index) {
	var Sentence = "";
	

	switch(parts) {
	case #:	while(wordLine.charAt(index) != null) {
			Sentence = Sentence + wordLine.charAt(index);	
		}

}
